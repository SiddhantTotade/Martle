import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  order: [],
};

export const placeOrderCartSlice = createSlice({
  name: "placeOrderCartSlice",
  initialState,
  reducers: {},
});
